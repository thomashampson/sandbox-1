pipeline {
  agent none

  stages {
    stage('Build and Test') {
      matrix {
       agent {
         label "${PLATFORM}"
       }
       axes {
         axis {
           name 'PLATFORM'
           values 'linux-64'
         }
       }
       stages {   
         stage('One') {
           steps {
             sh 'cat jenkins/info.txt'
           }
         }
         stage('Two') {
           steps {
             sh 'cat jenkins/info.txt'
           }
         }
       }
      }
    }
  }
}
